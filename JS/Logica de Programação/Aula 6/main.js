// Operação Ternaria

// (condição) ? 'valor para verdadeiro' : 'Valor para falso'

const pontuaçãoUsuario = 999;
const nivelUsuario =
  pontuaçãoUsuario >= 1000 ? "Usuario VIP" : "Usuário normal";

/*
O exemplo acima faz exatamente a mesma coisa que o exemplo a baixo

if (pontuaçãoUsuario >= 1000) {
  console.log("Usuário VIP");
} else {
  console.log("Usuário Normal");
}

*/
